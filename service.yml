apiVersion: v1
kind: Service
metadata:
  name: kuard-service
spec:
  type: NodePort
  selector:
    app: kuard
  ports:
    - protocol: TCP
      port: 8080


kind: Service
metadata:
  name: recipe
spec:
  type: NodePort
  selector:
    app: recipe
  ports:
    - protocol: TCP
      port: 8080
      